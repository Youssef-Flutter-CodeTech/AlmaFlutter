import '../../domain/entities/singup_entity.dart';

class SingupModel extends SingupEntity {
  SingupModel() : super();

  factory SingupModel.fromJson(Map<String, dynamic> json) {
    // TODO: Map JSON to model
    return SingupModel();
  }
}
